# Changelog

## 1.0.1

### Fixed

- `StateValueEmitter.onValue` now replays the current value to new subscribers.

## 1.0.0

- Initial release.
- `ValueEmitter` and `StateValueEmitter` classes.
- `useValueEmitter` hook.
